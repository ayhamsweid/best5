server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://api:4000/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /uploads/ {
    proxy_pass http://api:4000/uploads/;
    proxy_set_header Host $host;
  }

  location = /sitemap.xml {
    proxy_pass http://api:4000/sitemap.xml;
    proxy_set_header Host $host;
  }

  location = / {
    proxy_pass http://api:4000/api/seo/render;
    proxy_set_header X-Original-URI $request_uri;
    proxy_set_header Host $host;
  }

  location ^~ /admin {
    try_files $uri /index.html;
  }

  location ~ ^/(ar|en)/preview(/|$) {
    try_files $uri /index.html;
  }

  location ~ ^/(ar|en)(/|$) {
    rewrite ^ /api/seo/render break;
    proxy_pass http://api:4000;
    proxy_set_header X-Original-URI $request_uri;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location / {
    try_files $uri @seo_fallback;
  }

  location @seo_fallback {
    rewrite ^ /api/seo/render break;
    proxy_pass http://api:4000;
    proxy_set_header X-Original-URI $request_uri;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
